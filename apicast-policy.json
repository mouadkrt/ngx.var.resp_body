{
  "$schema": "http://apicast.io/policy-v1/schema#manifest#",
  "name": "Log response body",
  "summary": "",
  "description": "Munisys custom policy for logging response body",
  "version": "0.1",
  "configuration": {
    "type": "object",
    "properties": {
      "logfile_name": {
        "description": "Logfile name. Eg name of the WS itself, so that each WS logs in a seperate file",
        "type": "string"
      }
	}
  }
}